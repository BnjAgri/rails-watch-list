<h2>Add a movie to <%= @list.name %> list</h2>
        <%= simple_form_for [@list, @bookmark] do |f| %>
          <%= f.association :movie %>
          <%= f.submit class: "btn btn-primary" %>
        <% end %>

